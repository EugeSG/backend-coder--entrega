<div class="products">
    {{#if products.length}}
        {{#each products}}
            <div class="product">
                <div class="title">
                    Título: {{title}}
                </div>
                <div>
                    Descripción: {{description}}
                </div>
                <div>
                    Precio: {{price}}
                </div>
            </div>
        {{/each}}
    {{else}}
        <div>Aún no hay productos</div>
    {{/if}}
</div>